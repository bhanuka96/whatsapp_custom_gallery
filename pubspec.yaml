name: wechat_assets_picker

environment:
  sdk: ">=2.9.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

  extended_image: ^1.5.0
  photo_manager: ^0.6.0
  provider: ^4.3.3
  video_player: ^1.0.1
